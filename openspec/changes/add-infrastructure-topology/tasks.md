# Tasks

## 1. Инвентаризация текущего контура

- [ ] 1.1 Снять фактические характеристики каждой из 4 ВМ текущего контура (vCPU, RAM, диски, тип носителя, разметка, версия ОС) и сохранить вывод `lscpu`, `free -h`, `lsblk`, `vgs`, `lvs`, `df -h` по каждому узлу в `docs/manuals/inventory-dev.md`; проверка — в файле есть блок по каждому из узлов `dev-backend`, `dev-data`, `dev-infra`, `dev-backup`
- [ ] 1.2 Сверить фактические характеристики с заявленными в спеке (2/4/100, 4/8/150, 2/4/100, 1/1/500) и зафиксировать в том же файле раздел «Расхождения»; проверка — раздел присутствует и либо перечисляет расхождения, либо явно содержит «расхождений нет»
- [ ] 1.3 Подтвердить, что диски `dev-data` и `dev-infra` действительно размечены в LVM и имеют свободное место в VG под расширение; проверка — вывод `vgs` в `docs/manuals/inventory-dev.md` показывает ненулевой `VFree` либо зафиксировано обоснование его отсутствия
- [ ] 1.4 Зафиксировать фактическую раскладку сервисов по узлам текущего контура (что где реально запущено); проверка — таблица «узел → сервисы» в `docs/manuals/inventory-dev.md` покрывает все работающие сервисы

## 2. Документ состава серверов

- [ ] 2.1 Создать `docs/10-инфраструктура.md` с разделами «Текущий контур», «Целевой контур», «Раскладка ролей», «Хранилище и ёмкость», «Резервное копирование», «Сеть и изоляция», «Реестр разрывов», «Порядок пересмотра»; проверка — все восемь разделов присутствуют и непусты
- [ ] 2.2 Заполнить реестр узлов текущего контура по результатам задачи 1.1 в формате паспорта узла (идентификатор, контур, роли, vCPU, RAM, диски, носитель, разметка, площадка, владелец эксплуатации); проверка — ни одно поле ни у одного узла не содержит «уточняется»
- [ ] 2.3 Заполнить реестр узлов целевого контура по таблице из спека (`prod-app-01/02`, `prod-db-01/02`, `prod-kafka-01..03`, `prod-infra-01`, `prod-backup-01`) с теми же полями паспорта; проверка — девять узлов описаны полностью
- [ ] 2.4 Перенести в документ расчёты ёмкости из `design.md` (Р-3 СУБД, Р-4 шина, Р-10 резервный узел) с указанием исходных цифр ТЗ; проверка — для каждого объёма диска в реестре целевых узлов виден путь расчёта от параметров п. 7.1 и 7.4 ТЗ
- [ ] 2.5 Описать раскладку ролей по физическим хостам (Р-9) и правило запрета совмещения ролей в продуктиве; проверка — таблица «физический хост → узлы» присутствует, и по ней видно, что отказ любого одного хоста сохраняет кворум шины и одну из копий СУБД
- [ ] 2.6 Добавить `docs/10-инфраструктура.md` в таблицу состава документации в `docs/README.md`; проверка — ссылка открывается и описание строки соответствует содержимому

## 3. Резервное копирование, сеть и пороги

- [ ] 3.1 Описать в `docs/10-инфраструктура.md` схему резервного копирования продуктива: суточная полная копия, архив WAL с интервалом ≤ 15 мин, глубина хранения 12 месяцев, схема «дед — отец — сын»; проверка — из описания выводятся заявленные RPO ≤ 15 мин и RTO ≤ 2 ч
- [ ] 3.2 Описать ослабленную схему резервного копирования dev-контура с явной фиксацией RPO до 24 часов; проверка — в документе указано, что этот RPO принят осознанно и на продуктив не распространяется
- [ ] 3.3 Описать регламент квартальной проверки восстановления на изолированном стенде с формой фиксации фактических RTO и точки восстановления; проверка — форма протокола добавлена в `docs/test-protocols/`
- [ ] 3.4 Описать сетевые требования: перечень допустимых направлений доступа к СУБД, шине и сервисам наблюдаемости, терминирование TLS на обратном прокси, пропускная способность межузловой сети ≥ 1 Гбит/с; проверка — в документе есть матрица «источник → назначение → порт → основание»
- [ ] 3.5 Зафиксировать пороги расширения ресурсов (диск 70 %, CPU 60 % устойчиво за сутки, RAM 80 %) и указать, какой сигнал мониторинга им соответствует; проверка — для каждого порога назван конкретный сигнал и получатель уведомления

## 4. Реестр разрывов и материалы для закупки

- [ ] 4.1 Составить реестр разрывов между текущим и целевым контуром (описание, затронутое требование ТЗ, последствие, уровень риска, этап закрытия по Приложению Б); проверка — реестр покрывает как минимум отсутствие реплики СУБД, совмещение СУБД и шины на `dev-data`, единственный брокер шины, отсутствие архивирования WAL и размещение на VPS вместо on-premise
- [ ] 4.2 Подготовить целевой профиль оборудования для перечня ТМЦ этапа 2 (3 вычислительных хоста + хост резервного копирования с характеристиками из Р-9); проверка — профиль оформлен отдельным разделом, пригодным к переносу в закупочные материалы без пересчёта
- [ ] 4.3 Описать порядок пересмотра состава: триггеры (изменение НФТ, новый источник, смена СУБД или средства очередей, расхождение фактических метрик с расчётными более 30 %) и результат пересмотра; проверка — для каждого триггера указано, какие разделы документа обновляются

## 5. Сверка и приёмка артефакта

- [ ] 5.1 Сверить документ со сценариями спека `infrastructure/server-topology` и убедиться, что каждый сценарий имеет опору в тексте документа; проверка — составлена таблица «сценарий спека → раздел документа», непокрытых сценариев нет
- [ ] 5.2 Подготовить программу нагрузочной проверки целевого контура по сценариям спека (пик 500 соб/с не менее 15 минут, отказ узла приложения, отказ основного сервера СУБД) с критериями прохождения; проверка — программа добавлена в `docs/test-protocols/` и содержит измеримые критерии для каждого сценария
- [ ] 5.3 Прогнать `openspec validate "add-infrastructure-topology" --strict` и устранить ошибки (предупреждения о RFC 2119 для русскоязычного текста допустимы); проверка — команда не сообщает ошибок
